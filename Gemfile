# frozen_string_literal: true
source "https://rubygems.org"

# gems for the database, task runner, file access and IRB shell
gem "activerecord", "~> 5.2.3", :require => 'active_record'
gem 'sinatra-activerecord', :require => 'sinatra/activerecord'
gem 'rake'
gem 'require_all'
gem 'sqlite3', '~>1.3.6'
gem 'pry'

# gems for API reading, geocoding and using the .env (to hide API) if you would like
gem 'rest-client'
gem 'json'
gem 'geokit'
gem 'dotenv'

# gems for putting up the banner when you start the app
gem 'rmagick'
gem 'catpix'

# gems for formatting and styling
gem 'tty-prompt'
gem 'pastel'
